# Aplicação
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:4200

# MySQL (alinhado ao docker-compose.yml da raiz)
DATABASE_URL="mysql://techx:techxpass@localhost:3306/techx_tasks"

# MongoDB (alinhado ao docker-compose.yml da raiz)
MONGODB_URI="mongodb://mongo:mongopass@localhost:27017/techx_metadata?authSource=admin"

# JWT (preencher na Fase 2)
JWT_SECRET="troque-este-segredo-em-producao"
JWT_EXPIRES_IN="1d"
